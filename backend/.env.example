# Copy this file to .env (or just export the variable in your shell) and
# fill in your real key. NEVER commit a real key to git.
#
# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-key-here
